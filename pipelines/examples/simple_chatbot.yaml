name: simple_chatbot
version: "1.0"
description: A minimal chatbot that responds using a single LLM call
trigger: chat

nodes:
  - id: respond
    type: llm
    provider: openai
    model: gpt-4o-mini
    system_prompt: "You are a helpful, concise assistant."
    prompt: "{{input}}"
